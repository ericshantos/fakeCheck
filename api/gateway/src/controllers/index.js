const { checkNewsController } = require("./check.controller");
const { creditsController } = require("./credits.controller");
const { healthController } = require("./health.controller");
const { infoController } = require("./info.controller");

module.exports = {
    checkNewsController,
    creditsController,
    healthController,
    infoController
};